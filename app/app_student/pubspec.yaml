name: app_student
description: "AI 助学 · 学生端：受学习围栏约束的 AI 学习聊天。"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  app_core:
    path: ../app_core
  gpt_markdown: 1.0.19
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  shared_preferences: ^2.2.0 # 仅测试桩使用（SharedPreferences.setMockInitialValues）

flutter:
  uses-material-design: true
